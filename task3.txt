1) find file.sh выдаст название файла, если файл есть, и ошибку, если его нет
так же для текущего каталога можно воспользоваться конструкцией ls | grep file.sh
если необходимо проверить так же подкаталоги, то find -depth -name "file.sh"

2) chown user:users file.sh

3) chmod 730 file.sh
